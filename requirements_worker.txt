# Core dependencies for Celery and Redis
celery
redis

# Data fetching and processing libraries
pytrends
requests
pandas

# Optional ML/NLP libraries (include only if your worker tasks need them)
transformers>=4.15.0
torch
accelerate>=0.26.0
sentencepiece
numpy
